# git-task
add all the change in your directory to git
git add . 
commint your change with proper
save a snapshot of the changes in the staging area with descriptive message
git commit -m ""

#pushing your change into remote branch and remote origin
git push orgin [branch]

it will show the status of current respository including the file 
git status
git commit -m "write any message"